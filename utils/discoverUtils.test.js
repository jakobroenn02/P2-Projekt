
/*
const discoverUtils = require('./discoverUtils');

// Test shuffleArray function
const testArray = [1, 2, 3, 4, 5];
test('Returns a shuffled version of input array, that contains the same elements and is the same size', () => {
    expect(discoverUtils.shuffleArray(testArray).length).toBe([1, 2, 3, 4, 5].length);
    expect(discoverUtils.shuffleArray(testArray)).toContain(1, 2, 3, 4, 5);
});

*/


